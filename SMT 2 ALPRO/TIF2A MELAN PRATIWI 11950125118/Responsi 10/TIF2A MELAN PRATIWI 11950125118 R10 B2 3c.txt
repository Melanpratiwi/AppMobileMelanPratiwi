Start 
	int[]angka =  {82,12,41,38,19,26,9,48,20,55,8,32,3}

	print "Angka sebelum diurutkan menggunakan selection sort adalah : "

	selectionSort(angka)

	print "Angka setelah diurutkan menggunakan selection sort adalah : "

	int[] arr
for
	(int indeks=0; indeks < arr; indeks++)
	int minIndex = indeks
for
	(int indeks2= indeks + 1; indeks2 < arr; indeks2++)
if
	(arr[indeks2] < arr[minIndex])
	minIndex = indeks2

if
	(minIndex != indeks)

	int tmp = arr[indeks]
	arr[indeks] = arr[minIndex]
	arr[minIndex] = tmp

	String msg

		print(msg)
for
	(int i : arr)
		print (i + ", ")
EndIf
End