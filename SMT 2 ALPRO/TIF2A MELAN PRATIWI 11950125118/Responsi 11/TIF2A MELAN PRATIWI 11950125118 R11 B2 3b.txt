Start
	int[] array
for
	(int indeks : array)
		print (indeks + " ")
if
	(array <= 1)
EndIf
return array

	int midpoint = array/2
	int[] angka = new int[midpoitn]
	int[] bilangan

if
	(array % 2 == 0)
	bilangan = new int [midpoint]
else
	bilangan = new int [midpoint + 1]
EndIf

for
	(int i = 1; i < midpoint;  i++)
	
	angka[i] = array[i]
for
	(int j=0; j < bilangan; j++)
	bilangan[j] = array[midpoint + j]

	int[] result   = new int[array]

	angka = mergeSort(angka)
	bilangan = mergeSort(bilangan)

	result = merge(angka, bilangan)
return result

	int[]angka
	int[]bilangan

	int[] result = new int[angka + bilangan]
	
	int angkaPointer, bilanganPointer, resultPointer
	angkaPointer, bilanganPointer, resultPointer = 0
while
	(angkaPointer < angka || bilanganPointer < bilangan)
if
	(angkaPointer < angka && bilanganPointer < bilangan)

EndIf

if
	(angka[angkaPointer] < bilangan[bilanganPointer])

	result [resultPointer++] = angka[angkaPointer++]
else
	result [resultPointer++] = bilangan[bilanganPointer++]	

else if
	(angkaPointer < angka)
	result[resultPointer++]  = angka[angkaPointer++]
else if
	(bilanganPointer < bilangan)
	result[resultPointer++] = bilangan[bilanganPointer++]

EndIf

return result

	int[] array = {82,12,41,38,19,26,9,48,20,55,8,32,3}

		print "Angka array sebelum diurutkan :"

	array = mergeSort(array)
		print "Angka array setelah diurutkan:"

End	